import { DirectiveTitleDirective } from './directive-title.directive';

describe('DirectiveTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveTitleDirective();
    expect(directive).toBeTruthy();
  });
});
